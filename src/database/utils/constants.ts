export enum MongoCollection {
    USERS = 'users',
    PRODUCT = 'prouducts',
}
export enum CategoryCollection {
    USERS = 'users',
    CATEGORY = 'categorys',
}
export enum RoleCollection {
    USERS = 'User',
    Admin = 'Admin',
}
